# merge-worktree 快速笔记

**日期**: 2026-05-06
**维度**: user
**本次总结记录数**: 1

## 新增问题

| 类型 | 问题描述 | 次数 | 严重程度 |
|------|----------|------|----------|
| 错误 | 阶段 4A 强制要求切换到 main worktree，未考虑 GitHub Actions 发布型项目无需本地 main | 1 | major |

## 改进建议

1. **阶段 4A 增加发布脚本类型判定** — 先读 CLAUDE.md 和 publish.sh 内容，区分 GitHub Actions 触发型（`gh workflow run`）和本地版本 bump 型。前者在当前 feature worktree 直接运行，后者才切到 main worktree
2. **bare repo + worktree 适配** — 当需要切到 main worktree 时，用 `git worktree list | awk '/\[main\]/ {print $1}'` 查找 main 的实际路径，而非假设 `cd <main-worktree>` 可直达

## 相关记录

- raw-20260506_120000.json
